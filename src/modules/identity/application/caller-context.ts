/** Transport adapters use this tiny factory so application services receive a trusted caller explicitly. */
export { callerFromIdentity } from "../domain/caller-context";
export type { CallerContext } from "../domain/caller-context";
